# Frontend — copy to .env before npm start / npm run build
# React embeds REACT_APP_* at build time; change this before production builds.

# Backend HTTP API (WebSocket URL is derived from this automatically)
REACT_APP_API_URL=http://localhost:8000

# Production example:
# REACT_APP_API_URL=https://your-backend.onrender.com
